/// @file
/// @brief Task1: Single-file executable C++ program using a std::vector<std::vector<int>>
/// compile & run command:
///   mkdir -p build && g++ -std=c++20 src/task1.main.cpp -o build/task1 && ./build/task1

/// @todo Include standard library headers as needed

/// @brief Calculate the sum of all integer values in a set of sequences
/// @param data A set of sequences of values passed as a std::vector<std::vector<int>>
//    (assertion: at least one integer value is present)
/// @return Sum of all values
/// @todo Implement the function spec above as a funtion `sum`

int main()
{

    /// @todo
    /// 1. Create and prepare a local variable of type std::vector<std::vector<int>>
    ///   holding 9 int values in this arrangement (three rows with 3 values each):
    ///   [1, 2, 3]
    ///   [4, 5, 6]
    ///   [7, 8, 9]
    /// 2. Call your function 'sum' with this variable as argument.
    /// 3. Capture the result of your function call in a local variable.
    /// 4. Print the result to the console.

    return 0;
}
